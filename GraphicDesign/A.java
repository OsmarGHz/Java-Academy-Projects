//import javax.swing.ImageIcon;
//import javax.swing.JFrame;
//import java.awt.Color;
public class A {

    public static void main(String[] args) {
        // JFrame frame = new JFrame();
        // frame.setSize(420, 420);
        // frame.setTitle("Hola mundoooooo!");
        // frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //Exit the application
        // //frame.setDefaultCloseOperation(JFrame.HIDE_ON_CLOSE); //Hide the application
        // //frame.setDefaultCloseOperation(JFrame.DO_NOTHING_ON_CLOSE); //Hide the application
        // frame.setResizable(false);
        // frame.setVisible(true);

        // ImageIcon icon = new ImageIcon("minRecycle.png"); //ImageIcon creation
        // frame.setIconImage(icon.getImage());
        // frame.getContentPane().setBackground(new Color(0, 0, 0));

        
    }
}